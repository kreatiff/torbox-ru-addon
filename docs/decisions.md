# Decisions log

Every decision made so far that wasn't spelled out unambiguously in
[`torbox-ru-addon-spec.md`](../torbox-ru-addon-spec.md), organized by area. Each entry names the
spec section it responds to (where one exists) and where to find it in code. Items still
awaiting the repo owner's explicit sign-off are marked **PENDING**; everything else has already
shipped as described. The original proposals for the Tier-1/2/3 items are in
[`torbox-ru-addon-plan.md`](../torbox-ru-addon-plan.md) §3 — this file additionally tracks what
actually happened once implementation started, including a few decisions the plan didn't
anticipate.

## Where this disagreed with the spec

| Decision                                                                                                                                                        | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Spec ref      | Status                                                                                                                              |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| Manifest declares `"types": ["series"]`, not `["series", "movie"]`                                                                                              | Movies are an explicit v1 non-goal (§8) and the pipeline is series-first throughout; advertising a type that always returns empty is the kind of silent gap the spec elsewhere insists on surfacing. Not marked LOCKED, unlike its neighbors.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | §5.5, §8      | Confirmed with the repo owner and built (Milestone 3)                                                                               |
| No WebDAV FUSE mount inside the app container                                                                                                                   | `TORBOX_MOUNT_PATH` is optional and off the streaming critical path entirely — playback goes through `requestdl` by `torbox_file_id`, never a filesystem path. `docker-compose.yml` can bind-mount a host-level WebDAV mount read-only instead if one exists outside Docker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | §5.2, §9      | Not built yet; `files.mount_path` column exists, nothing populates it                                                               |
| Manifest's `idPrefixes` is `["tt", "tmdb:"]`, not just `["tt"]`; `stream.ts` resolves both id shapes (`titlesRepo.findByTmdbId` added alongside `findByImdbId`) | **Bug found installing the addon in real AIOStreams, against the repo owner's real account.** §5.5's id example (`tt1234567:3:8`) is IMDb-only, and the manifest declared only `idPrefixes: ["tt"]` — but AIOStreams resolved a real show (Большой куш / "Сокровища императора") via **TMDB** instead (`tmdb:250793:3:3`) and, per the Stremio addon protocol, filtered this addon out client-side before ever sending the request — nothing reached the app's own logs at all, which is what made this look like a request/mapping problem at first rather than a manifest one. `titles.tmdb_id` already existed in the schema (§4) but had no reader. `stream.ts` now exports a pure `parseStreamId(raw)` that recognises both `imdbId:season:episode` and `tmdb:tmdbId:season:episode` and dispatches to the matching repo lookup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | §5.5          | `src/http/routes/addon/manifest.ts`, `stream.ts`, `src/db/repositories/titlesRepo.ts`                                               |
| `/play/:fileId` still 302s straight to whatever `requestdl` returns, unchanged, despite a real gap found in what that protects                                  | **Found verifying Milestone 3 against the live account, not anticipated by the plan.** `client.ts`'s existing docstring (written in Milestone 1) assumed `requestdl`'s `Location` header is "a signed, short-lived CDN URL" — distinct from `TORBOX_API_KEY`. Calling it for real shows otherwise: the CDN URL's own `token=` query param **is** `TORBOX_API_KEY` itself, verbatim, not a separate scoped credential. The LOCKED text's literal ask — never let _our own_ Stream/response URLs carry the key — still holds: the addon only ever hands Stremio `${PUBLIC_BASE}/${token}/play/:fileId`, so the key never reaches Stremio's stream-list cache, Nuvio, or anywhere a _stream gets shared_ (the exact vector §5.5 names). What it can't prevent is the actual video player that follows that redirect seeing the key in the final CDN hop — a client our own code never controls. Closing that gap for real would mean proxying video bytes through this server instead of redirecting, which §5.5's own route shape ("302 → signed TorBox URL") doesn't call for and this host's bandwidth almost certainly can't absorb for a whole library. Left as a known, narrower residual exposure rather than silently claimed as fully closed — flagging for the repo owner's awareness, not a Milestone 3 blocker. | §5.5 (LOCKED) | Confirmed against the real account (`/play/:fileId` → real 302 → real `tb-cdn.io` URL containing the real key); no code change made |

## Tooling & infrastructure (spec explicitly left these to the implementer)

| Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | File(s)                                                           |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------- |
| **node-pg-migrate**, not Drizzle, for migrations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | §4's DDL is already final-ish with load-bearing comments; an ORM means maintaining a second schema representation that has to stay bit-for-bit synced with SQL that already says exactly what it means.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `migrations/`                                                     |
| Migration bodies are near-verbatim `pgm.sql(...)` wrapping §4's DDL, not the fluent builder API                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Same translation-risk reasoning, one level down — re-deriving already-correct DDL through a builder API risks a subtle mismatch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `migrations/1785725438965_init-schema.ts`                         |
| Hand-written repositories over plain `pg`, zod-validated at every read boundary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Gets the type-safety win of an ORM without the schema-sync risk; the spec already mandates zod for the TorBox client (§5.1), so this extends the same discipline to reads from Postgres.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `src/db/repositories/`, `src/db/schema.types.ts`                  |
| **Vitest**, split into a fast path (no external deps) and integration tests gated on `TEST_DATABASE_URL`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | TS/ESM-native, good fixture-table DX. Originally planned around `testcontainers-node`; see below for why that changed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `vitest.config.ts`, `test/`                                       |
| ~~testcontainers-node~~ → plain `TEST_DATABASE_URL` env var                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Deviation from the original plan.** The build sandbox's egress policy blocks Docker Hub entirely, so testcontainers (which needs to pull a Postgres image) can't run there. Integration tests instead point at any reachable Postgres 16 via `TEST_DATABASE_URL` and skip themselves (`describe.skipIf(!hasTestDb)`) when it's unset. Revisit testcontainers if tests ever need to run somewhere with no pre-existing Postgres available.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `test/db/testDb.ts`, `test/setup.ts`                              |
| `vitest.config.ts` sets `fileParallelism: false`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Not anticipated in the plan.** Discovered empirically: multiple test files sharing one real Postgres and each truncating tables in `beforeEach` will race each other under Vitest's default cross-file parallelism, causing spurious FK-violation and count-mismatch failures. Serializing test files fixed it; the suite is small enough that this costs nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `vitest.config.ts`                                                |
| `pg`'s bigint columns (`torbox_id`, `size`, bigserial ids) parsed as JS `number`, not string/BigInt                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Every bigint column here holds a "normal sized" number (a row id, a byte count) nowhere near `Number.MAX_SAFE_INTEGER`; parsing as `pg`'s default string would force BigInt/string-handling through every size comparison and sort for no real benefit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `src/db/typeParsers.ts`                                           |
| `gen_random_uuid()` defaults added to the two uuid primary keys (`titles.id`, `rules.id`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | §4's DDL doesn't say how ids get generated. Postgres 16 has `gen_random_uuid()` built in since PG13 — simplest option, and repository code never has to generate ids itself.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `migrations/1785725438965_init-schema.ts`                         |
| CHECK constraints added on `torrents.status`, `rules.numbering`/`sort`/`source`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | The TS union types for these already exist verbatim in the spec (§3.7, and the `torrents.status` comment) — the constraints enforce an invariant the spec already documents, not a new one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | same migration                                                    |
| Milestone 1's "app" is a one-shot batch job (migrate → ingest once → exit), not a long-running server                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | The build order (§6) doesn't introduce `src/http` until Milestone 3, and ingest _scheduling_ is explicitly Milestone 6 — a persistent process with nothing to schedule and no server to run would be either idle or misleading about what's implemented. `docker-compose.yml`'s `app` service currently has `restart: "no"` for the same reason.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `src/index.ts`, `docker-compose.yml`                              |
| Postgres has no `ports:` mapping in `docker-compose.yml`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Only `app` needs to reach it, over the compose-internal network; nothing about Postgres should be reachable from the Cloudflare Tunnel once that's wired up (Milestone 3+).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `docker-compose.yml`                                              |
| `pool.ts`'s `Pool` has an explicit `.on('error', ...)` handler; `index.ts`/`runOnce.ts` wrap `migrateUp()` in the same try/catch as `runIngest()`, not left bare                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Bug found and fixed during real Docker verification**, not anticipated in the original plan. `pg.Pool` is an `EventEmitter`; an `'error'` event with no listener crashes the whole process instead of surfacing as a normal rejection. With `migrateUp()` outside the try/catch, a DB-unreachable-at-boot condition (confirmed with a real `docker run` against a stopped Postgres) produced a raw uncaught-exception stack trace instead of the same clean, logged, exit-code-1 failure every other error path in this codebase produces.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `src/db/pool.ts`, `src/index.ts`, `src/ingest/runOnce.ts`         |
| `docker-compose.yml`'s `app` service lists `TORBOX_API_KEY`/`DATABASE_URL`/`TORBOX_REQUEST_DELAY_MS`/`LOG_LEVEL` as explicit `${VAR}` entries under `environment:`, not `env_file: [.env]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Deviation from how Milestone 1 originally shipped this**, prompted by the repo owner wanting to deploy via Portainer's Stacks UI. `env_file:` requires the named file to exist; a stack deployed from this repo via Portainer/Swarm/CI never has a `.env` on disk to point at (it's gitignored, by design, and was never pushed). Bare `${VAR}` interpolation is satisfied by a stack's own "Environment variables" UI, by the shell running `docker compose up`, or — unchanged for existing local setups — by a project-root `.env` file, which Compose auto-loads for interpolation independently of whether any service declares `env_file:`. `DATABASE_URL`/`TORBOX_REQUEST_DELAY_MS`/`LOG_LEVEL` default the same way `postgres`'s credentials do (row above); `TORBOX_API_KEY` deliberately has no default — left unset, `docker compose config` only warns, but the container itself still fails loudly via `config.ts`'s existing validation. Verified with `docker compose config` in three states: normal `.env`, no `.env` with the key exported in the shell (stands in for a Portainer stack variable), and neither. | `docker-compose.yml`                                              |
| Images published to **GHCR** (`ghcr.io/kreatiff/torbox-ru-addon`) via `.github/workflows/docker-publish.yml` on every push to `dev`, `linux/arm64` only; a new `docker-compose.ghcr.yml` (image-only, no `build:`) added alongside the source-building `docker-compose.yml`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | The repo owner asked to publish an image so their deployment host never has to clone the repo. Couldn't be done directly from a build session: no registry credentials are available there, and its Docker daemon can't actually build `linux/arm64` (QEMU emulation doesn't work in that sandbox either — see the Milestone 1 note above), which would be the wrong architecture for the real, locked deployment target anyway. GitHub's own runners do real multi-arch builds via `docker/setup-qemu-action`, so the workflow builds there instead, using the Actions-provisioned `GITHUB_TOKEN` (`packages: write` explicitly requested — recent repos default that scope to read-only). `docker-compose.yml` itself is untouched (still builds from source for local dev); `docker-compose.ghcr.yml` is the new, source-free file meant to be pasted directly into a stack UI or run standalone. GHCR package visibility (public vs. private, and whatever pull credential that implies for the deployment host) is still the repo owner's call — see README's Portainer section.                                                | `.github/workflows/docker-publish.yml`, `docker-compose.ghcr.yml` |
| `Dockerfile`'s `build` stage runs `npm ci --ignore-scripts`, not plain `npm ci`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Bug found and fixed verifying the GHCR workflow end-to-end** (first real `dev` push): the build stage's `npm ci` installs full `devDependencies`, which pull in esbuild/rollup native binaries transitively (via `vitest`, `tsx`) even though this stage only ever runs `tsc`. Under GitHub Actions' QEMU-emulated `linux/arm64` build, one of those packages' postinstall executing its downloaded arm64 binary crashed with `qemu: uncaught target signal 4 (Illegal instruction)`, hanging the job (no error surfaced to the run/job status, just a stuck step). `--ignore-scripts` skips all install-time lifecycle scripts; safe here since nothing in this stage's dependency tree needs install-time compilation (confirmed: `npm ci --ignore-scripts && npm run build` still produces byte-identical `dist/` output). The `runtime` stage's `npm ci --omit=dev` was never affected — it excludes `devDependencies` entirely, so esbuild/rollup are never installed there.                                                                                                                                                  | `Dockerfile`                                                      |
| Both compose files' `postgres` healthcheck adds `start_period: 60s`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Bug found deploying the GHCR stack to the real Portainer/Oracle host**: with no `start_period`, Docker counts every failed `pg_isready` from the moment the container starts toward the `retries: 10` × `interval: 5s` budget (~50s total) before marking it unhealthy — and Compose's `depends_on: condition: service_healthy` then aborts the whole stack deploy (`dependency failed to start: container ... is unhealthy`). First-ever boot against an empty `pgdata` volume runs `initdb` and an internal restart before Postgres answers `pg_isready` at all, which can plausibly take longer than 50s on the target host (Oracle Ampere A1 free-tier block storage). `start_period` only changes how early failures are tolerated, not how fast a success is detected, so this has no cost on a healthy fast path. Not fully confirmed as the _only_ cause (repo owner's actual Postgres container logs from that failed deploy weren't available at the time of this fix) — worth revisiting if a stack still reports unhealthy after this with `start_period` exhausted too.                                               | `docker-compose.yml`, `docker-compose.ghcr.yml`                   |
| Milestone 3 (`src/http`) ships **addon routes only** — `/manifest.json`, `/stream/series/:id.json`, `/play/:fileId` — no admin/UI routes (Milestone 4) and, despite `src/index.ts`'s own superseded Milestone-1-era comment implying otherwise, no ingest scheduler either (still Milestone 6, per the row above on why Milestone 1 shipped one-shot). Fastify chosen per plan.md §5.5/§5.6 ("Fastify for HTTP. No Stremio addon SDK"); `:token` checked via `crypto.timingSafeEqual` over SHA-256 digests of both sides (equal-length by construction, so it can't throw on a length mismatch the way a raw compare of the two strings would, and doesn't leak the real token's length either) — a bad/missing token is a 404, matching the LOCKED "never confirm the route exists" framing. `titlesRepo` ships **read-only** (`findByImdbId` only) — `findOrCreate` and anything UI-authored stays Milestone 4 scope, per the existing row on that below. `app` changes shape from Milestone 1's one-shot batch job to a persistent server accordingly (`docker-compose.yml`'s `app.restart` → `unless-stopped`, plus a `ports:` mapping); ingest itself stays manual (`npm run ingest` / `docker compose exec app npm run ingest`) until Milestone 6. New required config: `ADDON_TOKEN`, `PUBLIC_BASE`; new optional: `PORT` (default `3000`), `NOT_WEB_READY_EXTENSIONS` (comma-separated, default `ts` — spec asks for "a per-file computed flag with an env override" on `notWebReady`; this makes the computation's _inputs_ configurable rather than a blunt global on/off switch). | `src/http/`, `src/index.ts`, `src/config.ts`, `docker-compose.yml`, `docker-compose.ghcr.yml`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

## Schema additions beyond §4's literal DDL

| Decision                                                                                                                      | Why                                                                                                                                                                                                                                                                                                                                                                 | Spec ref             | Status                                                                                                                |
| ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | --------------------------------------------------------------------------------------------------------------------- |
| Additive `provider_seasons` table (title_id, season, source, episode_count, episodes jsonb, fetched_at) + `titles.poster_url` | §5.3's confidence scoring, §5.6's validation banners, §3.5.3's air-date extractor stage, and the show picker's "per-season episode counts" all need per-season provider data that §4 has nowhere to put, even though §5.4 explicitly says to cache provider lookups in Postgres. Keyed by `(title, season, source)` so TMDB and TVDB each keep an independent view. | §4, §5.3, §5.4, §5.6 | Implemented (`migrations/1785725439402_provider-cache.ts`); **PENDING** sign-off since §4 was called "close to final" |
| Nullable-unique constraints on `titles.tvdb_id`/`titles.tmdb_id` (matching the existing `imdb_id` constraint)                 | Only `imdb_id` was unique in §4, and it's nullable — meaning the shows _most_ likely to lack one (the exact case the spec calls out) had zero duplicate protection. `titlesRepo.findOrCreate` (Milestone 4) still needs to actually use this.                                                                                                                       | §4, §9               | Implemented; **PENDING** sign-off                                                                                     |

## `src/resolve/expandRule` (Milestone 2)

| Decision                                                                                                                   | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Spec ref                                                                        | Status                           |
| -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | -------------------------------- | ----------- |
| `sequential` = sort, then `episode = startEpisode + index`                                                                 | The deliberate human-override mode: don't try to interpret filenames, just count them in sorted order.                                                                                                                                                                                                                                                                                                                                                                                                                        | §3.7 (type shape only, not semantics)                                           | Implemented                      |
| `continuous` = sort, then `episode = startEpisode + index − absoluteOffset`, treating `startEpisode` as an absolute number | **Revised from the original plan.** The plan assumed this mode needed the (not-yet-built) extractor cascade to find each file's absolute episode number; implementing it revealed it's actually pure position/offset arithmetic, assuming a contiguous run of episodes within the torrent — true of every example in the spec. Shipped in Milestone 2 instead of waiting for Milestone 5. A per-file `absolute_hint` from the real cascade would only matter for a non-contiguous torrent, which no current example requires. | §3.5, §3.7                                                                      | Implemented                      |
| `manual` = only files with an explicit `exceptions` entry get a mapping; everything else is dropped, no fallback           | Distinguishes it from the other three modes, which all have _some_ positional/parsed fallback for non-excepted files.                                                                                                                                                                                                                                                                                                                                                                                                         | §3.7                                                                            | Implemented                      |
| `parsed` throws a clear "not implemented, needs the extractor cascade" error                                               | Real per-file text parsing (SxxExx / word+number / air-date / positional fallback) is Milestone 5 scope (§6: "do not skip ahead"). Fails loudly rather than guessing.                                                                                                                                                                                                                                                                                                                                                         | §3.5, §6                                                                        | Stubbed, throws by design        |
| `exceptions` (including `'ignore'`) resolved once, before mode dispatch, and excluded from the position count              | Removing a trailer must not shift every episode number after it — the position count only ever sees files that don't already have an explicit answer.                                                                                                                                                                                                                                                                                                                                                                         | §5.6 ("ignore this file" toggle)                                                | Implemented                      |
| Non-video files never produce a mapping, filtered inside `expandRule` itself                                               | A domain rule intrinsic to what "expand a rule into episode mappings" means; keeps every caller from having to remember to pre-filter.                                                                                                                                                                                                                                                                                                                                                                                        | —                                                                               | Implemented                      |
| `expandRule` sorts by `rawPath`, not `mountPath`, even when the latter is set                                              | Mount discovery isn't built yet and rarely populated; `rawPath` is always available. Worth reconsidering once mount discovery (an optional pass, still unbuilt) is common.                                                                                                                                                                                                                                                                                                                                                    | §5.2, §9                                                                        | Implemented, flagged for revisit |
| `rulesRepo.getRuleById`/`listRules` throw if `torrent_hash`/`title_id` is null                                             | §4's DDL has no `NOT NULL` on these FK columns (transcribed verbatim), but a rule with either unset can't be materialised into anything meaningful. Enforced once, at the read boundary, rather than pushing `                                                                                                                                                                                                                                                                                                                | null`through`Rule` and every numbering function that doesn't otherwise need it. | §4                               | Implemented |
| `rebuildAllMappings()` wired into the end of every ingest run                                                              | §5.2 names "rebuild mappings" as a pipeline step. "Propose rules" (the step before it) doesn't exist yet, so this currently just reprocesses whatever rules already exist — hand-inserted ones for now, UI-authored ones from Milestone 4. One rule failing (most likely `numbering: 'parsed'`) is logged and skipped rather than aborting the run.                                                                                                                                                                           | §5.2                                                                            | Implemented                      |

## Milestone 4 review (Labeller UI, admin API, TMDB client)

Built on a `milestone4` branch, then reviewed and fixed in a second pass before merge (not a
rebuild — same branch, same author of the fixes as this log's other entries), and merged to
`dev`.

| Decision                                                                                                                                                                        | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Spec ref                  | Status                                         |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ---------------------------------------------- |
| `ADMIN_USER`/`ADMIN_PASS` (config.ts) have no default, matching `ADDON_TOKEN`'s existing precedent; both compose files and `.env.example` updated to actually pass them through | **Bug found in review.** The initial version defaulted both to `admin`/`admin` with no compose wiring to override them at all — a real vulnerability, not a dev convenience, since `/admin` and `/api` sit behind the same public Cloudflare Tunnel as the addon (§5.5's own reasoning for why `ADDON_TOKEN` gets no default applies identically here).                                                                                                                                                                                                                                                                                                                                                                                                                                                              | §5.5, §5.6                | Fixed                                          |
| UI build runs in its own `FROM --platform=$BUILDPLATFORM` Dockerfile stage, native output copied into the final image                                                           | **Bug found in review, verified via a real CI build on a throwaway branch.** The initial version ran `npm ci` for the UI inside the same `linux/arm64`-emulated stage as the backend — but Vite _executes_ esbuild/rollup to bundle (`vite build`), it doesn't just install them, so the existing `--ignore-scripts` fix (which only helps install-time postinstall scripts, see the Milestone-3-era Dockerfile entry above) doesn't cover this case. Building natively on the runner's own architecture and copying the static, architecture-independent output in sidesteps the QEMU crash class entirely. First attempt at this fix caught a second, unrelated real bug: the isolated stage didn't have the sibling `src/resolve/` files `ui/src/App.tsx` imports directly (`COPY src /app/src` added to fix it). | —                         | Fixed, confirmed via a real GitHub Actions run |
| `POST /api/rules` rejects `numbering: 'parsed'` with a 400 before writing anything                                                                                              | **Bug found in review.** `expandRule` throws for `'parsed'` by design (Milestone 5, not built yet — see the expandRule table below) but the API's zod schema still accepted the value; a request reaching `rebuildMappingsForRule` after the rule row was already committed would surface a bare 500 with a mapping-less rule left in the DB. The Labeller UI already disables this `<option>`, but that's not a substitute for the API enforcing its own contract.                                                                                                                                                                                                                                                                                                                                                  | §6 (do not skip ahead)    | Fixed                                          |
| `/api/rules` checks `provider_seasons` for a cache hit before calling `fetchSeasonDetails`                                                                                      | **Bug found in review.** The initial version fetched TMDB season data live on every save regardless of whether it was already cached, contradicting §5.4's "cache aggressively... these change rarely" — meaningful cost for a one-torrent-per-episode show, where every episode's rule save would re-fetch the same season data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | §5.4                      | Fixed                                          |
| The Labeller UI's "Provider Mismatch" episode-count banner was removed, not fixed                                                                                               | **Dead code found while replacing `App.tsx`'s ~18 `any` types with real interfaces**, not part of the original review. It read `selectedShow.seasons`, but `TmdbSearchResult` (what `/api/titles/search` actually returns) never carries season data — no route exists to fetch per-season episode counts client-side — so the banner was always a silent no-op, unreachable under any real input. Real per-season TMDB data was already being fetched server-side (for the cache above) but never exposed to the client; wiring that through is a small feature addition outside a lint-driven fix pass, not a bug fix, so it wasn't added here.                                                                                                                                                                    | §5.6 (validation banners) | Removed                                        |
| Root `eslint.config.js`'s ignore list changed from `src/ui/dist/**` to `ui/**`                                                                                                  | The plan document originally nested the UI under `src/ui/`; the actual implementation put it at top-level `ui/` instead, but the ignore pattern was never updated to match, so root `eslint .` tried (and failed) to load `ui/`'s own separate config/plugin tree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —                         | Fixed                                          |
| `admin/index.ts` fails loud (logged warning + real 404) instead of `mkdirSync`-ing a phantom directory when no UI build is found                                                | **Bug found in review.** The initial version's static-path resolution used a non-null-asserted array index and silently created an empty directory on disk if the Vite build output was missing, which would mask a genuinely broken deployment (e.g. the Docker UI build stage failing) as an empty-but-present `/admin`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | —                         | Fixed                                          |
| Test coverage added for `/api/rules` (including the `parsed`-rejection and TMDB-cache-hit paths), `/api/torrents/:hash`, `/api/library`, and `/api/health`                      | The initial version's `test/http/api.test.ts` only covered Basic Auth and `/api/titles/search` — the core CRUD/rebuild logic added this milestone had zero automated coverage.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | —                         | Fixed; suite grew from 76 to 84 tests          |

## Editable mapped rules (post-Milestone-4 follow-up)

| Decision                                                                                                                                                                                                                                           | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Spec ref | Status                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------- |
| Editing an already-mapped rule from the Library view reuses `POST /api/rules` with a new optional `ruleId` field, rather than a separate PUT/PATCH endpoint                                                                                        | The save/rebuild logic (title resolution, TMDB season caching, `rebuildMappingsForRule`) is identical for "create" and "edit" — only whether a `titleId` is already known differs, and that was already an optional input. `GET /api/torrents/:hash` now also returns any existing `rules` for that hash (title joined in) so the Labeller can pre-fill from one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | §5.6     | Implemented                                                                                                   |
| Editing a rule into a different season deletes the old rule row first (`rulesRepo.deleteRule`, cascades to its mappings) before upserting the new one                                                                                              | `rules`'s unique constraint is `(torrent_hash, season)` (§4) — `upsertRule`'s `ON CONFLICT` target is that pair, so changing the season doesn't overwrite the row being edited, it just inserts a new one alongside it, leaving the old rule (and its now-stale mappings) behind as an orphan. The API checks the submitted `ruleId`'s current season against the new one and deletes it first only when they differ, and only after confirming the rule actually belongs to the submitted `torrentHash`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | §4       | Implemented                                                                                                   |
| The Labeller pre-fills `titleId` directly (skipping `findOrCreateTitle`/TMDB external-id lookup) when editing without changing the show, but falls back to the normal `title: {...}` payload the moment the user picks a different show via search | Re-resolving external ids from TMDB on every edit would be wasted work when the show itself isn't changing — the title's DB id is already known from the rule being edited. Picking a new show clears that stashed id client-side, so the server treats it exactly like a fresh rule.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | §5.4     | Implemented                                                                                                   |
| `admin/index.ts`'s UI-build path collapsed from a dev/prod pair to one computation                                                                                                                                                                 | **Real bug found verifying this feature in a browser via local dev mode (`npm run dev`, not Docker).** The previous "pick whichever of two paths exists" logic always resolved to `<repo-root>/ui` (the Vite _source_ tree, whose committed `index.html` references unbundled `/src/main.tsx`) whenever running outside Docker, even after a real `cd ui && npm run build` — because that source `index.html` is always present in git, so the existence check never actually distinguished "was a build done" from "does the source checkout exist." Since this file lives at the same depth from the repo root whether running compiled (`dist/http/routes/admin/index.js`) or from source via `tsx` (`src/http/routes/admin/index.ts`), and the Vite build always lands at `<repo-root>/dist/ui` either way, one path computation is correct for both. Never manifested in the Docker image (which never has the raw `ui/` source tree, only `dist/ui`), which is why the CI-verified Docker build didn't catch it. | —        | Fixed                                                                                                         |
| Deleting a rule/mapping for a torrent that's gone from TorBox is still **not built**                                                                                                                                                               | The Health tab already surfaces `goneCount`/`goneTorrents`/`danglingMappings` (Milestone 4), but purely as read-only diagnostics — nothing calls `rulesRepo.deleteRule` for this case, even though it already does the right cascade (used today only by the season-change edit path above). Noted here rather than silently left as a gap the repo owner would have to discover by trying it in the UI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | §5.6     | Not built — no `DELETE` route or UI action exists yet                                                         |
| Library's episode-coverage grid: green boxes are exactly one file mapped to that episode, orange (`.duplicate`) is more than one, empty/bordered-only is a gap                                                                                     | Distinguishes "an intentional multi-source episode" (e.g. the two-overlapping-torrents case above) from "a numbering mistake mapped an extra file" at a glance — hovering a box's tooltip lists the actual matched filename(s) either way. Was already implemented in the Milestone 4 UI; recorded here since it came up as a real question and wasn't documented anywhere.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | §5.6     | Implemented (`ui/src/index.css`'s `.episode-box.mapped`/`.duplicate` rules, `ui/src/App.tsx`'s `LibraryView`) |

## `src/torbox` client (Milestone 1)

| Decision                                                                                                                                                                 | Why                                                                                                                                                                                                                                                                                                                                              | Spec ref                                             | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `envelope.ts` checks `success` before ever touching `data`, on every response                                                                                            | The spec's own warning: `{"success":false,...,"data":null}` looks identical to an empty library if `success` isn't checked first. Logs the raw body on any failure — both `success:false` and a `success:true` body that fails its schema.                                                                                                       | §5.1                                                 | Implemented, **confirmed against a live response**: a real (invalid-token) request to `api.torbox.app` returned exactly `{"success":false,"error":"BAD_TOKEN","detail":"...","data":null}`, parsed correctly                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Response field names in `schemas.ts` are a best-effort reconstruction                                                                                                    | Originally written with no network path to `api.torbox.app` available; every field optional/nullable where uncertain so a mismatch degrades to a logged gap instead of a crash.                                                                                                                                                                  | §5.1                                                 | **Confirmed against a real account.** A real `TORBOX_API_KEY` was supplied and `getMylist()` parsed a live 210-torrent/2067-file payload through `mylistResponseSchema` without a single schema-mismatch log line. The real library also turned out to contain the exact spec §1 example torrents verbatim (`Сокровища императора...`, `Ставка на любовь...`, the `Bolshoy.Kush...(1080р)` homoglyph case), which round-tripped through Postgres correctly.                                                                                                                                                                                 |
| `files` on a mylist torrent is `undefined` when absent, never defaulted to `[]`                                                                                          | Its _absence_, not emptiness, is the signal to fall back to a per-torrent `?id=` fetch.                                                                                                                                                                                                                                                          | §5.1                                                 | Implemented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Per-torrent fallback fetches are rate-limited (serial + fixed delay via `TORBOX_REQUEST_DELAY_MS`, default 250ms); torrents with inline `files` are not throttled at all | TorBox doesn't publish a rate limit, so a conservative default was chosen rather than invented confidence; only calls that actually hit the API need throttling.                                                                                                                                                                                 | §5.1 ("Rate-limit yourself")                         | Implemented, default tunable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `getPlaybackUrl` fetches `requestdl` with `redirect: 'manual'` and reads the `Location` header itself                                                                    | The whole point is handing that URL to `/play/:fileId`'s own redirect — the app must never follow through to the CDN itself, and must never hand the client a URL containing `TORBOX_API_KEY`. Written now (Milestone 1's "TorBox client" scope) even though nothing calls it until Milestone 3.                                                 | §5.5 (LOCKED: never return a URL containing the key) | Implemented, unused until Milestone 3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `refreshWebdav()` is best-effort: failures are logged and swallowed, never abort ingest                                                                                  | The spec doesn't say how the WebDAV host authenticates (it may not be the same bearer token as the REST API), and force-refresh is an optimisation — the view auto-refreshes every 15 minutes regardless.                                                                                                                                        | §5.1, §9                                             | Implemented, and the "unconfirmed auth mechanism" guess turned out right: with a real `TORBOX_API_KEY`, `POST /refresh/` still 405s (`Allow: PROPFIND`) under Bearer auth _and_ under Basic auth using the API key as username or password. A `PROPFIND` against the WebDAV host confirms why — it 401s with `WWW-Authenticate: Basic realm="TorBox WebDAV"`, a **separate credential from `TORBOX_API_KEY`** that this repo doesn't have. Handled as a non-fatal warning either way, so ingest is unaffected; force-refresh itself needs real WebDAV Basic-auth credentials from the repo owner (TorBox account settings) to ever succeed. |
| `markAbsentGone` refuses to do anything when given an empty present-list                                                                                                 | An empty `mylist` response is exactly as ambiguous as `{"success":false,...,"data":null}` — could be a real empty library or a transient API problem. Treating it as "mark everything gone" would be the single worst failure mode the spec names (§9: a wrong mapping that looks like it worked), just applied to every torrent instead of one. | §9                                                   | Implemented, covered by a dedicated test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `is_video` computed from a fixed file-extension allowlist (mp4, mkv, ts, avi, webm, mov, wmv, m4v, flv, mpg, mpeg)                                                       | Not specified by the spec; a reasonable default, easy to extend if a real library turns up something missing.                                                                                                                                                                                                                                    | —                                                    | Implemented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## Milestone 5 pre-implementation sign-offs (auto-proposal engine)

The confidence-materialisation and confidence-formula items were flagged as the real gate in
`milestone5-plan.md` §3, blocking the start of step 4 (`src/resolve/confidence.ts` +
`proposeRule.ts`). Both confirmed by the repo owner before implementation starts, and the
formula item's own review pass surfaced two additional categorical gates (title match, `X из Y`
mismatch) beyond the originally-scoped positional one — all recorded below. Steps 1–3
(`normalize/`, `extract/`, fixtures) are unaffected by any of this and can proceed in parallel.

**Revised after a review pass found three real gaps in the first draft** (missing title-match
validation, a spec-contradicting treatment of `X из Y` mismatches, and a scoring conflict for
partial-season torrents — see the table below for what changed and why). The table below is the
current, corrected version; nothing from the first draft survives unchanged except the
materialisation-tiers row and the `proposal_reason` column decision.

| Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Spec ref      | Status                                                                                                                                                               |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Confidence materialisation: three-way split confirmed** — high → insert rule + `rebuildMappings` immediately (live, no flag); medium → insert + `rebuildMappings` but set `⚠` in stream description; **any categorical gate below fires** → insert the rule but **withhold** `rebuildMappings` entirely (rule exists but is inert — shows in Queue, produces no streams until a human accepts it). Originally scoped as a single positional-fallback gate; widened to three gates (see below) since a wrong-show match and a self-contradicting `X из Y` count are at least as dangerous as pure positional fallback, and the first draft didn't catch either.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Precisely the §9 failure mode: a rule that is inert-but-visible is infinitely safer than one that is live-but-wrong. The `⚠` at medium tier is already read from `rules.confidence` by the stream description logic (shipped in Milestone 4) — no new wiring needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | §5.3, §9      | **Confirmed**                                                                                                                                                        |
| **`rules.proposal_reason text` column added (additive migration)** — `proposeRule`'s human-readable explanation string is persisted in a new nullable column on `rules`, not recomputed on demand. Same pattern as `raw_name_at_ingest`: an immutable snapshot of what was true at the moment the decision was made. Recomputing on demand would silently drift if `provider_seasons` is refreshed after the proposal is made.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Cheap (one migration, no existing columns touched), consistent with how the rest of the schema treats "what was true when this row was created," and removes the ambiguity in what a Queue entry's `why` field is describing — it will always describe the evidence that was present at proposal time, not the evidence available right now.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | §5.3          | **Confirmed** — additive migration required before step 5 wires `proposeRule` into the ingest pipeline                                                               |
| **Categorical gate #1 (new) — no confident title match.** Force QUEUE regardless of episode-level score if neither of: (a) the torrent's normalised name exactly matches an existing `titles` row's `name_ru`/`name_en`/an alias (reusing a show already confirmed elsewhere is stronger evidence than a fresh search hit); or (b) a fresh `searchTitles` call's top result's normalised name matches the cleaned torrent name exactly or within a small punctuation/whitespace-only tolerance, **and** no other result in the same search is within that same tolerance of the top result (an unambiguous match, not just top-ranked). Anything short of that leaves `titleId` unset and forces QUEUE, same as today's manual-required path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Every other signal validates episode _numbering_; none validated _which show_ got matched — a wrong-show match could sail through every episode-level check (file count coincidentally matches the wrong show's season, every file gets an SxxExx) and reach HIGH. This is spec §9's named worst failure mode verbatim ("a plausible-but-wrong [id]... maps files onto a real show's episode list and looks like it worked"), just arriving via title search instead of TorBox's own tags.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | §5.3, §9      | **Confirmed** — new gate, not in the first draft                                                                                                                     |
| **Categorical gate #2 (new) — `X из Y` present and mismatched.** Force QUEUE regardless of episode-level score when the name declares a completeness count that doesn't equal the actual video file count, even if every file otherwise has a clean SxxExx match. No `X из Y` in the name → gate doesn't apply (no effect either way).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Spec §3.4 (LOCKED) is binary, not a scoring suggestion: "X... must equal the video file count. When it does, mark high-confidence... When it doesn't, **queue for review and say why**." The first draft only credited a _match_ (+0.35) and never penalised a mismatch beyond withholding that one signal — not strong enough given the spec's own "must"/"queue" wording. A mismatch here is a real red flag (wrong release, an extra or missing file, a same-name re-upload), not a partial-credit miss.                                                                                                                                                                                                                                                                                                                                                                                                                                                 | §3.4 (LOCKED) | **Confirmed** — new gate, not in the first draft                                                                                                                     |
| **Categorical gate #3 (revised) — positional fallback dominates.** Force QUEUE if more than half of the torrent's in-scope video files needed cascade stage 5 (positional/natural-sort fallback) for an episode number. Revised from "stage 5 was the _only_ stage that matched" (literally 100%) to a >50% threshold.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | The all-or-nothing version had a real loophole: a torrent with 12/13 files cleanly SxxExx-matched and 1/13 falling back to positional dodged the gate entirely on a technicality (not literally _all_ positional) and landed as a mere −0.25 score penalty — nowhere near the safety margin a 13/13-positional torrent gets. A majority threshold closes that without needing an exact accidental-match rate to tune against.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | §5.3, §9      | **Confirmed** — revised from the first draft's all-or-nothing version                                                                                                |
| **Weighted formula for proposals that pass all three gates.** `score = clamp01( sum(fired weights, including negative penalties) / sum(max-possible weights of applicable positive signals) )` — clamped to [0, 1] so a penalty can never push the score negative in a way that changes tier logic. HIGH: score ≥ 0.75 **and** at least one High-weight signal fired. MEDIUM: score ≥ 0.45 (and not HIGH). QUEUE: everything else. Signal weights: every file has an explicit SxxExx match (High, +0.30); file count == `X из Y` total, when present (High, +0.35); file count == `provider_seasons.episode_count`, **applicable only when no partial `X из Y` is declared** (i.e. no count present, or X == Y) (High, +0.35) — when the torrent explicitly declares a partial count (X < Y), this signal is inapplicable (excluded from both numerator and denominator) and replaced by: every extracted episode number is a member of `provider_seasons.episodes` for that season (High, +0.35); `absolute_hint` consistent with the cumulative offset implied by SxxExx (Medium, +0.15); air-date lookup matched ≥ 1 file against `provider_seasons.episodes` (Medium, +0.15); fraction of in-scope files needing positional fallback (Low penalty, linear from 0 at 0% to −0.25 at the ~50% gate-#3 boundary — so 1 positional file out of 13 takes roughly a −0.04 hit, not the first draft's flat −0.25 regardless of how few files were affected). | Fixes two gaps in the first draft. First, the provider-episode-count signal and the `X из Y` signal were both High-weighted and _mutually exclusive_ for exactly the content this system targets: a correctly-labelled partial-season torrent (the real Сокровища императора case, spec §1) matches `X из Y` but structurally can never match a full season's provider count — capping its achievable score below an equally well-labelled full-season torrent's. Scoping the provider-count signal to the non-partial case and substituting an episode-membership check for the partial case keeps both kinds of correctly-labelled torrent able to reach the same ceiling. Second, a flat penalty regardless of how many files were affected either over-punishes one accidental fallback or under-punishes a proposal that dodges gate #3 at 51%; scaling it to the actual affected fraction tracks the real risk instead of a single trigger condition. | §5.3, §3.4    | **Confirmed** — exact thresholds (0.75, 0.45, the ~50% gate boundary, per-signal weights) subject to fixture-driven tuning; shape and the gate/signal set are locked |
